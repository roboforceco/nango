integrations:
    notion:
        notion-pages:
            runs: every half hour
            returns:
                - NotionPage

models:
    NotionPage:
        id: string
        url: string
        content: string
        parent_page_id: string | undefined
